.claude/
.idea/
.assumeIdentity.json
bashrc.d/startup-files/.env
/.env
.env

# App build artifacts
app/node_modules/
app/dist/
app/dist-portable/
app/src-tauri/target/

app/*.tsbuildinfo

# Distribution artifacts -- GitHub Releases is canonical; the local `releases/`
# folder is the package-portable.mjs output dir but is not committed.
releases/
